export default function MockRegister() {
  return (
    <div
      style={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        background: 'var(--bg)',
      }}
    >
      <div style={{ textAlign: 'center', color: 'var(--text)' }}>
        <h2>Registration not available in demo</h2>
        <p>This feature is disabled in the demo environment.</p>
      </div>
    </div>
  );
}
